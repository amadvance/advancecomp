#!/bin/bash

(
	export CFLAGS="-O3 -march=i586 -mcpu=i686 -fomit-frame-pointer" ;
	export CXXFLAGS="-O3 -march=i586 -mcpu=i686 -fomit-frame-pointer" ;
	./configure --host=i386-mingw32msvc --build=`./config.guess` $@
)
